const btn=document.querySelector(".theme-toggle button");const label=btn.querySelector(".label-text");const themes=["light","dark-mode","blueprint","grayscale"];let currentIndex=0;const themeLabels={"light":"Light","dark-mode":"Dark","blueprint":"Blueprint","grayscale":"Grayscale"};label.textContent=themeLabels[themes[(currentIndex+1)%themes.length]];btn.setAttribute("aria-label",`${themeLabels[themes[(currentIndex+1)%themes.length]]} mode`);btn.addEventListener("click",()=>{document.documentElement.classList.remove(...themes.filter(t=>t!=="light"));currentIndex=(currentIndex+1)%themes.length;const newTheme=themes[currentIndex];if(newTheme!=="light"){document.documentElement.classList.add(newTheme)}const nextTheme=themes[(currentIndex+1)%themes.length];label.textContent=themeLabels[nextTheme];btn.setAttribute("aria-label",`${themeLabels[nextTheme]} mode`)});